const ENTER = "Enter";
const ALL = "all";
const ACTIVE = "active";
const COMPLETED = "completed";

const KEY = {
  ENTER,
};

const FILTER = {
  ALL,
  ACTIVE,
  COMPLETED,
};
export { KEY, FILTER };
