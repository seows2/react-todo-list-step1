const ENTER = "Enter";
const ESCAPE = "Escape";
const ESC = "Esc";
const ALL = "all";
const ACTIVE = "active";
const COMPLETED = "completed";
const CONTENTS = "contents";

const TODO_PROPERTY = {
  COMPLETED,
  CONTENTS,
};

const KEY = {
  ENTER,
  ESCAPE,
  ESC,
};

const FILTER = {
  ALL,
  ACTIVE,
  COMPLETED,
};
export { KEY, FILTER, TODO_PROPERTY };
